# Specs and design

Start with the behaviour spec: **[spec/START-HERE.md](spec/START-HERE.md)**.

Marketing pages for a running copy live in `web/marketing/` and are served at `/`,
`/how-it-works`, and `/for-agents`.

Appearance (Broadsheet) is in `Foundations.dc.html`, `Screens.dc.html`, and `recipient/`.
Spec owns what the product does. Design owns how it looks.
